
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	Попытка
	    ПутьКСправке = ВСД.ПолучитьПутьКСправкеВИКИ(ПараметрКоманды);
		Если ЗначениеЗаполнено(ПутьКСправке) Тогда
			ЗапуститьПриложение(ПутьКСправке); 
		КонецЕсли;	
	Исключение КонецПопытки;
КонецПроцедуры

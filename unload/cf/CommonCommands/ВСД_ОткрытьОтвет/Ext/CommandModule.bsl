&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	//ВСД.ОткрытьОтвет1СвМеркурий(ПараметрКоманды);
	Попытка
	    ИмяФайла = кб99_ВСД.ПолучитьЗначениеРевизитаОбъекта_НаСервере(ПараметрКоманды,"ИмяФайлаОтвет");
		Если ЗначениеЗаполнено(ИмяФайла) Тогда
			ЗапуститьПриложение(ИмяФайла); 
		КонецЕсли;	
	Исключение КонецПопытки;
КонецПроцедуры

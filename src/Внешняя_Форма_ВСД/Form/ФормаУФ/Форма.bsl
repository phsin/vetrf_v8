
&НаСервере
Функция кнПечатьНаСервере()
	ФОбъект= РеквизитФормыВЗначение("Объект");
	Возврат ФОбъект.ПечатьВнешнейПечатнойФормы(ДокСсылка, "", "");
	// Вставить содержимое обработчика.
КонецФункции

&НаКлиенте
Процедура кнПечать(Команда)
	табдок =кнПечатьНаСервере();
	ТабДок.Показать();
КонецПроцедуры

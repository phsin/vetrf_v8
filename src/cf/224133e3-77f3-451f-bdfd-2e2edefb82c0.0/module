
Процедура ПартияПриИзменении(Элемент)
	Продукция = Партия.Продукция;
	ВидПродукции = Партия.ВидПродукции;
	Продукция_Элемент = Партия.Продукция_Элемент;
	НаименованиеПродукции = Партия.НаименованиеПродукции;
КонецПроцедуры

Процедура кнТестНажатие(Элемент)
	//тестовая
	ЗапуститьПриложение("https://t2-mercury.vetrf.ru/pub/operatorui?_action=findVetDocumentFormByUuid&uuid="+UUID);	

КонецПроцедуры

Процедура кнРабочаяНажатие(Элемент)
	//рабочая
	ЗапуститьПриложение("http://mercury.vetrf.ru/pub/operatorui?_language=ru&_action=showVetDocumentFormByUuid&uuid="+UUID);	

КонецПроцедуры

Процедура ПриОткрытии()
	Если ЗначениеЗаполнено(Партия) или ЭтоНовый() Тогда
		ЭтаФорма.ЭлементыФормы.Панель1.Страницы.Страница2.Видимость = Истина;
	КонецЕсли;
	
КонецПроцедуры
